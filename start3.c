/*
 * This program contains a strategy module for a
 * Go Fish program which prioritizes the greatest
 * grouping of cards as the next card to be asked
 */

#include <stdio.h>

#include "gofish.h"
#include "extern.h"

/* Strategy-3
 *    select a card based on a prioritization scheme
 *    first check for groups of 3 cards, ask one of these
 *    then check for single cards, ask this
 *    and then anything else (groups of 2), ask this
 */
int Strategy3(int player)
{
    int card;

    // ... put code here ...

    return(card);
}
